import PropTypes from "prop-types";
import Admins from "./Admins";

const Cd = ({cd}) => {
    return (
        <tr className="cd">

        </tr>
    )
}

Cd.propTypes = {
   // cd:PropTypes.object,
}
export default Cd
